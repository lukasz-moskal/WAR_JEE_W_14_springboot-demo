
spring:
  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp

  datasource:
    url: jdbc:mysql://localhost:3306/springboot?serverTimezone=UTC
    username: root
    password: root


#  security:
#    user:
#      name: admin
#      password: admin
#      roles: ADMIN,USER

#spring.jpa.hibernate.ddl-auto=create
  jpa.generate-ddl: true
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 20MB

  mail:
    protocol: smtp
    host: smtp.gmail.com
    port: 587
    username: <tu trzeba wstawić użytkownka>
    password: <tu trzeba wstawić hasło>
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

logging.level.web: DEBUG
logging:
  level:
    org:
      springframework:
        security: DEBUG



